machine:
    python:
        version: 2.7.6

dependencies:
    override:
        - pip install -r server/requirements.txt

test:
    override:
        - python manage.py test:
            pwd: 
                server
