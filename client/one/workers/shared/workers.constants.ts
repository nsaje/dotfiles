export enum ActionTopic {
    FETCH_NAVIGATION = 'fetchNavigation',
}

export enum EntityType {
    CONTENT_AD = 'contentAd',
    AD_GROUP = 'adGroup',
    CAMPAIGN = 'campaign',
    ACCOUNT = 'account',
}
