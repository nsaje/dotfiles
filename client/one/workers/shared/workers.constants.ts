export enum ActionTopic {
    BUILD_HIERARCHY = 'buildHierarchy',
}

export enum EntityType {
    CONTENT_AD = 'contentAd',
    AD_GROUP = 'adGroup',
    CAMPAIGN = 'campaign',
    ACCOUNT = 'account',
}
