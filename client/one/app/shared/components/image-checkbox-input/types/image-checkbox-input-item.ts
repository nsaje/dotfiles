import {ImageCheckboxInputIcon} from '../image-checkbox-input.constants';

export interface ImageCheckboxInputItem {
    value: string;
    displayValue: string;
    icon: ImageCheckboxInputIcon;
    checked: boolean;
}
