import {ImageCheckboxInputIcon} from '../../image-checkbox-input/image-checkbox-input.constants';

export interface ImageCheckboxInputGroupItem {
    value: string;
    displayValue: string;
    icon: ImageCheckboxInputIcon;
}
