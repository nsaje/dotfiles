import {Browser} from '../../../core/entities/types/common/browser';
import {FieldErrors} from '../../../shared/types/field-errors';

export interface BrowserTargetingErrors {
    family: FieldErrors;
}
