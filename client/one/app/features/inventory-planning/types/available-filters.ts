import {AvailableFilterOption} from './available-filter-option';

export interface AvailableFilters {
    countries: AvailableFilterOption[];
    publishers: AvailableFilterOption[];
    devices: AvailableFilterOption[];
}
